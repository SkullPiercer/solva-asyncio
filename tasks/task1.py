# ЗАДАЧА 1
# Напишите корутину fetch_status(), которая отправляет GET-запрос по URL и возвращает HTTP-статус ответа.
# Используйте aiohttp.ClientSession и ключевое слово await.
# Затем создайте функцию fetch_all(), которая принимает список URL и запускает параллельно несколько запросов.
# Верните список статусов по каждому URL.

import aiohttp
import asyncio

async def fetch_status(session, url):
    pass

async def fetch_all(urls):
    pass
